Weight-ensembling mixture-of-experts methods extend traditional model merging by introducing dynamic routing over task-specific components. Instead of computing a fixed parameter combination, the model learns to select and combine relevant knowledge depending on the input instance. This allows the system to handle multiple tasks more effectively, especially when tasks have different characteristics or require different feature representations.